import { View, Text } from "react-native";

export default function Home() {
  return (
    <View className="flex-1 items-center justify-center bg-red-500">
      <Text className="text-xl font-bold text-white p-2">
        Setup pour executer une application mobile avec Expo-Router et NativeWind 
      </Text>
    </View>
  );
}